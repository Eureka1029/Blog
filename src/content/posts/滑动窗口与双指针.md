---
title: "算法 : 滑动窗口与双指针"
published: 2026-03-30
description: 关于滑动窗口与双指针
image: ""
tags:
  - 算法
category: 算法
draft: false
---
具体算法思想可参考该文章:
https://www.cnblogs.com/huansky/p/13488234.html


## 定长滑动窗口
```cpp
// 固定窗口大小为 k
    string s;
    // 在 s 中寻找窗口大小为 k 时的所包含最大元音字母个数
    int  right = 0;
    while(right < s.size()) {
        window.add(s[right]);
        right++;
        // 如果符合要求，说明窗口构造完成，
        if (right>=k) {
            // 这是已经是一个窗口了，根据条件做一些事情
           // ... 可以计算窗口最大值等 
            // 最后不要忘记把 right -k 位置元素从窗口里面移除
        }
    }
    return res;
```